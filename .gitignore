# Xcode / SwiftPM
.build/
.build/*
# 打包产物入库:build.noindex/PureView.app 作为发布包随仓库分发
# `.noindex` 后缀让 Spotlight / 启动台忽略这份工作副本，避免和应用目录里的安装包重复。
build/*
!build/.gitignore
build.noindex/*
!build.noindex/PureView.app
*.xcodeproj
xcuserdata/
.swiftpm/

# macOS
.DS_Store
._*

# Agent tooling artifacts
.mimosa/
.workbuddy/

# 本地工作文档:只留本地,不随仓库分发、不推 GitHub。
# (约束/计划/审查/分析类,统一放 docs/,新文档放进去即自动忽略。README.md 不属此类)
AGENTS.md
docs/

# Editor
.vscode/
.idea/
