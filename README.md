# PureView v0.7.7

macOS 原生图片查看器 · Swift 6 + SwiftUI(混合 AppKit) · 零第三方依赖 · 支持 61 种格式

> `PureView` 是 Pictool 的应用显示名（Bundle 仍为 `Pictool`），主打 **ApolloOne 式文件夹快翻 + 完整 EXIF + 顺手裁切 + 正经打印** 的 Mac 原生体验。

![macOS 14+](https://img.shields.io/badge/macOS-14%2B-blue) ![Swift 6](https://img.shields.io/badge/Swift-6-orange) ![License MIT](https://img.shields.io/badge/license-MIT-green)

**下载：** [Releases](https://github.com/dongsheng512/PureView/releases/latest) 取 `PureView-0.7.7.zip` 或 `.dmg`（macOS 14+）。源码自行构建见下方。

## ✨ 亮点（v0.7.7）

- **文件夹快翻**：多根侧栏 + 缩略图网格 + 触控板横滑切图，1000 张目标内存 < 500MB
- **完整 EXIF**：右侧信息抽屉（文件 / 图像 / 拍摄 / GPS），可折叠、一键复制
- **统一编辑**：裁切、文字、画笔（实线/荧光）、形状（矩形/椭圆/直线/箭头）、马赛克、橡皮同一工具条；导出 PNG/JPEG/HEIC/TIFF，默认可另存
- **原生体验**：磨砂侧栏、自绘红绿灯、可拖中线、正经打印（`NSPrintOperation`）
- **格式广度**：ImageIO 原生 61 种可解码（含 HEIC / WebP / AVIF / JXL / 全系 RAW）
- **图标**：深色渐变底 + 白色几何雪山 + 暖橙日（矢量绘制，任意尺寸锐利）

## 功能

- **文件夹浏览**：`NSOpenPanel` 多根项目侧栏 + 文件夹树懒加载 + `UTType.image` + `localizedStandardCompare` 自然排序
- **缩略图网格**：边栏下半区 `LazyVGrid` 自适应，`CGImageSourceCreateThumbnailAtIndex` 降采样 + `NSCache(900/80MB)` + 合并重复请求，当前项高亮并自动滚动跟随
- **流畅缩放**：`NSScrollView+NSImageView` 层托管 `CALayer(contents)`，触控板捏合 / `⌘/⌥+滚轮` 锚点缩放 / 拖拽平移 / 双击 `适配↔100%` / 横向主导滑动切图；大图按视口 `×2` 降采样，超阈值自动加载全尺寸无缝替换
- **图片信息**：`CGImageSourceCopyPropertiesAtIndex` 一站式 EXIF/TIFF/IPTC/GPS/色彩/位深/DPI/帧数 + 文件信息，`Inspector` 可折叠 + 一键复制
- **简单裁切**：8 手柄选区 + `自由/1:1/4:3/3:4/16:9/9:16` + 三分线 + 实时像素读数；`CGImage.cropping(to:)` 纯函数换算（单测覆盖），导出 `PNG/JPEG/HEIC/TIFF` 并尽量保留 EXIF
- **统一编辑**：预览式单行工具条（`C` 裁切 / `D` 标记），**文字**（连续字号,选中框角柄拖拽改字号）/ **画笔**（样式 chip:实线/荧光半透明）/ **马赛克·模糊**（笔迹蒙版）/ **形状**（矩形·椭圆·直线·箭头）/ **橡皮** / 裁切同一编辑器；画布缩放平移（捏合·⌘滚轮以指针为锚 `25%–800%`,滚轮/空格拖拽平移,底部状态栏百分比下拉,⌘0 适应）；方向键微调、右键菜单、整行撤销、会话级标注缓存；导出面板（格式/位置信息/水印实时预览）+ 分裂菜单覆盖原图（同格式源）,标记默认不写回原图
- **打印**：`NSPrintOperation` + `per-job PrintInfo(copy)` + `clip` 分页 + 零边距 + `fitScale` 自动横竖
- **格式广度**：ImageIO 原生 61 种可解码（HEIC/HEIF/AVIF/WebP/JXL/PSD/RAW 全系 CR2/NEF/ARW/RAF/RW2/ORF/DNG…）· 21 种可编码，`WebP/AVIF 只解不编` 已约束
- **交互**：`←→` 切图/`0 适配`/`1 实际`/`⌘=/⌘-` 缩放/`I 信息`/`C 裁切`/`F 纯净`/`⌘R 刷新`/`⌃⌘S 侧栏`，支持文件/文件夹拖入、外部用图定位

## 快捷键

| 按键 | 功能 |
|---|---|
| `⌘O` | 打开文件夹 |
| `← / →` | 上一张 / 下一张（首尾循环） |
| `0` / `1` | 适配窗口 / 实际大小 |
| `⌘=` / `⌘-` | 放大 / 缩小（光标为锚点） |
| `I` | 信息面板 |
| `F` / `Esc` | 只看图（沉浸）/ 退出 |
| `C` | 编辑（裁切工具） |
| `D` | 编辑（文字工具） |
| `T` / `B` / `M` / `E` | 编辑中切换 文字/画笔/马赛克/橡皮 |
| `⌘0` / `⌘=` / `⌘-` | 编辑画布 适应窗口 / 放大 / 缩小 |
| `⌘Z` / `⇧⌘Z` | 编辑中 撤销 / 重做 |
| `⌘⏎` | 编辑中打开导出面板 |
| `⌘P` | 打印 |
| `⌃⌘S` / `拖中线` | 显/隐侧栏 / 拖动调节 `180–400` |
| `⌘R` | 刷新当前文件夹 |

## 构建与运行

预编译包：见 [GitHub Releases](https://github.com/dongsheng512/PureView/releases/latest)。

```bash
./build.sh            # release 构建 + 组装 PureView.app（ad-hoc 签名）
open build/PureView.app

swift build           # debug 构建
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test   # 单元测试
```

> `XCTest` 不随 CommandLineTools 提供，需 `DEVELOPER_DIR` 指向完整 Xcode。要求 **macOS 14+ / Xcode 16+**。

## 结构

```
Sources/Pictool/
├── PictoolApp.swift              # @main + WindowGroup(hiddenTitleBar) + 菜单
├── Models/
│   ├── CanvasBackground.swift    # 画布背景偏好（UserDefaults）
│   ├── ImageFile.swift           # UTType 识别与自然排序
│   ├── FolderStore.swift         # 多根/选区/隐藏/LRU/预取（@Observable, MainActor）
│   ├── MarkupAnnotation.swift    # 标记图元 + MarkPalette + MarkupGeometry
│   ├── AnnotationStore.swift     # 进程内标记缓存
│   └── ThumbnailProvider.swift   # 缩略图 NSCache + 合并请求 + 精确计费
├── Views/
│   ├── MainContentView.swift     # 三栏布局 + 可拖中线 + 纯净层
│   ├── PureHeader.swift          # 32pt 自绘顶栏 + 分段磨砂 + 红绿灯
│   ├── ImageViewCanvas.swift     # NSScrollView+NSImageView 画布 + 动图
│   ├── SidebarView.swift         # 文件夹树 + 磨砂材质
│   ├── ThumbnailGridView.swift   # 自适应网格 + 虚线空态
│   ├── EditView.swift            # 统一编辑器（预览式工具条）
│   ├── CropView.swift            # 裁切画布 + 兼容入口
│   ├── MarkupView.swift          # 标记兼容入口
│   ├── InfoInspector.swift       # 信息面板
│   └── SettingsView.swift        # 设置
└── Services/
    ├── ImageLoader.swift         # 降采样/全尺寸解码 + DisplayImageCache(250MB)
    ├── CropService.swift         # 纯函数坐标换算 + 编码保留 EXIF
    ├── AnnotationRenderer.swift  # 标记预览/导出同一绘制
    ├── MarkupService.swift       # 标记烙印导出
    ├── WatermarkService.swift    # 标记导出水印
    ├── MetadataService.swift     # 格式化
    ├── ZoomMath.swift            # 锚点缩放/像素对齐（单测）
    ├── PrintService.swift        # 打印
    └── L10n.swift
Tests/PictoolTests/               # CropMath / MarkupGeometry / WatermarkLayout 等
```

## 版本

- **v0.7.7** (2026-09-05) — 重绘应用图标:深色渐变底 + 白色几何雪山 + 暖橙日(CoreGraphics 矢量绘制,任意尺寸锐利),替代低分辨率照片源;图标生成脚本 `Assets/make_icon.swift` 改为内置设计直出
- **v0.7.6** (2026-09-05) — 信息抽屉改为侧栏同款 `HStack` 条件兄弟(右缘滑入,占位挤开画布,不再盖图);切图过渡保持原样式(新图沿方向滑入+淡入,横滑超阈值直接切图)。中间试过的幽灵层/覆盖式推挽/跟手手势未合入
- **v0.7.1** (2026-09-05) — 图片信息抽屉改用侧栏同款过渡:整体从右缘滑入/滑出(0.22s easeInOut),画布连续重排,移除冻结跳变机制
- **v0.7.0** (2026-09-05) — 编辑器全面升级正式版:画布缩放平移(25%–800%,状态栏下拉+手势)、形状工具(矩形/椭圆/直线/箭头)、文字角柄连续字号、画笔样式(实线/荧光)、导出融合菜单+水印双入口、侧栏整体化动画与缩略图区默认展开;含双 agent 全量审查修复,118 项单测
- **v0.6.18** (2026-09-05) — 缩略图头部整行 hover 反馈(5% 高亮底,外留 4pt),替代箭头胶囊底作为可点暗示
- **v0.6.17** (2026-09-05) — 侧栏缩略图头部重排:标题计数合并为「缩略图 · N」,去掉分节图标与箭头胶囊底,整行可点切换展开(hover 高亮),行距收紧
- **v0.6.16** (2026-09-05) — 侧栏缩略图区默认展开(打开/切换文件夹即展开,手动复原后保持到下次切换)
- **v0.6.15** (2026-09-05) — 侧栏收放整体化:顶栏左段材质改为连续宽度动画(与侧栏列宽同曲线锁步,分界线是一条同步移动的竖缝),PureHeader 统一单布局分支,转场去透明度(磨砂材质不再透出画布)
- **v0.6.14** (2026-09-05) — 侧栏收放挂上缺失的动画通道(0.22s easeInOut,滑入+淡入真正生效),告别单帧硬切;分隔线拖拽宽度不受影响
- **v0.6.13** (2026-09-05) — 全量代码审查修复:顶栏切工具回写 editTool(C/D 菜单不再失效)、草稿与旋转/翻转/撤销互斥、裁切空点不再产生假撤销、删空列表退出编辑态、箭头头部像素空间修复横竖失真、效果缓存防串图(强持有 base + countLimit)、命中容差像素化、EXIF 剔除失效尺寸键、移动/角柄重绘 30ms 节流、角柄贴边可拖且按下不产生假撤销、切工具清手势残状态、箭头实时预览头部实心、删除死代码 MarkupService;新增 4 项单测(118 项全绿)
- **v0.6.12** (2026-09-05) — 缩放控制回归底部状态栏(与浏览模式同款百分比下拉:适配窗口/50%/100%/200%/400%),移除画布右下角悬浮胶囊;画布手势(捏合/⌘滚轮/空格拖)保留
- **v0.6.11** (2026-09-05) — 合并重复的画笔工具:荧光笔不再是独立工具,并入画笔的「样式」chip(实线/荧光),选中已有笔迹自动同步样式
- **v0.6.10** (2026-09-05) — 缩放下限放开到 25%(可缩到比适应窗口更小),画布加裁切不再盖住顶栏/底栏;水印按钮只留图标并与颜色/大小 chip、导出按钮统一高度
- **v0.6.9** (2026-09-05) — 修复缩放胶囊 −/+ 与 ⌘=/⌘- 失效(容器尺寸只在 onAppear 首次回传);导出按钮改为与水印 chip 同几何的自绘蓝色按钮,高度统一
- **v0.6.8** (2026-09-05) — 导出按钮改蓝色主按钮(隐藏菜单箭头,只显示「导出」),与水印 chip 统一高度
- **v0.6.7** (2026-09-05) — 编辑器退出按钮改为 ✕ 图标并移至最右(水印 | 导出 | ✕)
- **v0.6.6** (2026-09-05) — 回退导出菜单内联格式选择(格式在选项面板中已有),保留融合单菜单按钮(存储为…/覆盖原图)与 ⌘⏎ 直达面板
- **v0.6.5** (2026-09-05) — 导出融合为单一菜单按钮:格式四选一直接在菜单内勾选,存储为…/覆盖原图同菜单,⌘⏎ 直达导出面板
- **v0.6.4** (2026-09-05) — 顶栏恢复水印独立按钮(状态图标化,画布实时预览),导出弹层保留水印勾选;导出分裂菜单按钮收窄
- **v0.6.3** (2026-09-05) — 文字角柄拖拽连续改字号(B4,sizeLevel 迁移为 sizeFraction)+ 荧光笔工具(半透明宽笔,H 键),选中三档 chip 与连续字号联动
- **v0.6.2** (2026-09-05) — 顶栏收敛(B3):导出改分裂菜单(存储为…/覆盖原图),删独立红色按钮与水印齿轮,水印开关+设置挪进导出弹层
- **v0.6.1** (2026-09-05) — 形状工具(B2):矩形/椭圆/直线/箭头,拖拽创建、选中移动/微调、改色改粗、可擦可删,导出与预览同渲染;箭头实心三角头
- **v0.6.0** (2026-09-05) — 编辑画布缩放平移(B1):捏合/⌘滚轮以指针为锚缩放(1–8×),滚轮/空格拖拽平移,⌘0 适应窗口,右下角倍率胶囊,窗口 resize 视野稳定;裁切画布不缩放
- **v0.5.36** (2026-09-05) — 草稿框磨砂回退最轻档(ultraThin)、占位文字降到 60% 白,更通透
- **v0.5.35** (2026-09-05) — 草稿框磨砂加密一档(regular)、占位文字提到 85% 白
- **v0.5.34** (2026-09-05) — 修复 v0.5.33 草稿框撑满画布:按键监听是 NSViewRepresentable 无固有尺寸,补零尺寸 frame
- **v0.5.33** (2026-09-05) — 修复文字草稿:输入法组合期间占位文字与候选预览重叠,第一次按键即隐藏占位(本地 keyDown 监听,随草稿销毁)
- **v0.5.32** (2026-09-05) — 文字草稿占位文字降到 72% 白,保持清晰但不抢正文
- **v0.5.31** (2026-09-05) — 文字草稿框改深色磨砂材质 + 自绘白色占位文字(macOS 上 TextField prompt 颜色在 plain 样式下不生效,弃用)
- **v0.5.30** (2026-09-05) — 文字草稿提示语改纯白、底色加深到 60%,任何底图上都清晰可读
- **v0.5.29** (2026-09-05) — 文字草稿输入框提示语改中性白(60% 不透明度),底色加深到 50%,不再继承标注色导致看不清
- **v0.5.28** (2026-09-05) — 顶栏编辑按钮选中态改为 accent 填充、去掉重阴影(macOS 工具栏选中范式)
- **v0.5.27** (2026-09-05) — 修复 v0.5.26 闪退:cursorUpdate 里调 invalidateCursorRects 触发 AppKit 重入异常,悬停光标改由 mouseMoved/mouseExited 驱动
- **v0.5.26** (2026-09-05) — 编辑器打磨:画笔/马赛克可拖动、方向键微调标注、草稿输入不再被 C/D 菜单拦截、按工具光标与悬停手型、右键菜单(编辑/删除)、工具快捷键 T/B/M/E、切工具保留选中、选中框双层描边、马赛克预览节流、裁切弹层去重
- **v0.5.25** (2026-09-02) — 导出弹层两行左标签右控件对齐,存储按钮拉满居中
- **v0.5.24** (2026-09-02) — 导出弹层只保留格式和是否包含位置信息,去掉最长边/质量,窗口收窄
- **v0.5.23** (2026-09-02) — 导出弹层:格式菜单按内容收窄、最长边右对齐、GPS 改成独立「包含位置信息」
- **v0.5.22** (2026-09-02) — 编辑时收掉浏览入口;画布略深于顶栏底栏;底栏显示「适应窗口」;覆盖原图降权、退出弱化、撤销靠拢工具
- **v0.5.21** (2026-09-02) — 编辑条与顶栏同底同高,只留一条淡线分隔,去掉两层色块的割裂感
- **v0.5.20** (2026-09-02) — 顶栏与编辑条之间加淡实线;导出弹层改为对齐标签行+底栏存储按钮
- **v0.5.19** (2026-09-02) — 导出改为工具条小弹层选格式/质量/GPS/最长边,再弹出精简存储面板选路径
- **v0.5.18** (2026-09-02) — 编辑工具条「完成」改为「退出」,只退出编辑模式不写文件
- **v0.5.17** (2026-09-02) — 水印大小/平铺间距滑杆区间下调;主图预览仅在勾选「启用水印」后显示
- **v0.5.16** (2026-09-02) — 水印设置去掉弹层缩略图,预览只叠在主图上;点九宫格会关掉平铺,位置才能生效
- **v0.5.15** (2026-09-02) — 水印预览对齐导出:相对裁切后画幅绘制、长文缩进画幅、白字+投影不再描边错位;设置里九宫格定位与百分比;画布仅在启用或打开设置时预览
- **v0.5.14** (2026-09-02) — 文字标记不再描边,只用所选颜色实心填充
- **v0.5.13** (2026-09-02) — 文字标记回车后不再出现并排两份字:CoreText `CTLineDraw` 会推进 textPosition,描边和填充各画一次就会错位成「白描边 + 红实体」;改为一次填充+描边,编辑中的原文从叠层里拿掉,命中盒重新对上可拖
- **v0.5.12** (2026-09-02) — 去掉编辑工具条和画布之间那条透出窗口的 Divider 缝
- **v0.5.11** (2026-09-02) — 编辑时拖画布/九宫格不再拖走窗口:编辑会话锁住 `isMovableByWindowBackground`(侧栏分隔条和 chrome 重挂不能把它打开),裁切和标记都改走 AppKit `nextEvent` 跟踪循环吃掉 mouseDragged,SwiftUI `DragGesture` 不再把按下漏给窗口
- **v0.5.10** (2026-09-02) — 修复裁切拖选框变成拖整个窗口:窗口是 hiddenTitleBar + `isMovableByWindowBackground`,`NSView.mouseDownCanMoveWindow` 默认 true(重写 mouseDown 不会自动变 false),且 `hitTest` 传进来的点在父视图坐标系、原代码拿 bounds 直接判把大片区域漏给窗口;补 `mouseDownCanMoveWindow = false`、`hitTest` 先 `convert(_:from: superview)`,捕获层钉死整画布尺寸,拖到一半切工具时兜底恢复窗口可拖状态
- **v0.5.9** (2026-09-01) — 修复裁切选区拖动:比例预设是像素宽高比、选区是归一化坐标,两者漏了折算导致 16:9 图上碰一下手柄选区就塌掉约一半高、拖左/右边时纵向瞬移;角手柄改取「较大解」,沿单轴拖动不再纹丝不动;「自由」比例真正不锁形状(与「原始」区分开),预设比例贴合(含 1:1)现在裁出的像素比例正确
- **v0.5.5** (2026-09-01) — 水印烙进预览底图(设置面板缩略图+画布所见即所得);裁切原功能收进「裁切选项」下拉(变换/比例/拉直/全选重置/覆盖)
- **v0.5.4** (2026-09-01) — 水印设置内嵌效果预览,打开面板或启用后画布同步预览
- **v0.5.3** (2026-09-01) — 编辑在原窗口展开不换黑底、不额外缩小图片;顶栏单一编辑按钮(按下深阴影,再点退出);水印收成工具条一钮并在画布上实时预览
- **v0.5.2** (2026-09-01) — 编辑工具条跟顶栏同色系但更淡;颜色/字号粗细/马赛克效果/裁切选项改为预览式小菜单
- **v0.5.1** (2026-09-01) — 编辑改为主窗口内操作(不再弹 sheet),与预览.app 一样在原图位置加工具条;`C`/`D` 编辑中切换裁切/文字,Esc/完成退出
- **v0.5.0** (2026-09-01) — 裁切与标记合并为预览式统一编辑器:一条工具条(裁切/文字/画笔/马赛克/橡皮),属性在条上,旋转翻转跟内容走;导出一条管线(变换→烙印→裁切→水印);`C`/`D` 打开同一窗口停在不同工具;原有打码/水印/拉直/比例/覆盖原图均保留
- **v0.4.4** (2026-09-01) — 标记编辑器内直接弹出水印设置(模态 sheet 挡不住系统偏好窗口)
- **v0.4.3** (2026-09-01) — 标记导出格式改用原生下拉框(NSPopUpButton);水印从裁切挪到标记编辑器(底栏开关 + 导出烙印)
- **v0.4.2** (2026-09-01) — 标记导出保存面板的格式改为下拉选择
- **v0.4.1** (2026-09-01) — 标记/水印验收修复:文字命中盒改为归一化(可连续加点)、拖动保留抓取偏移、双击改字、Esc 分层(草稿/取消选中/关 sheet);会话缓存关 sheet 不丢;带入主视图旋转;文字描边;默认红色;马赛克最近邻硬边;导出可选去掉 GPS;水印坐标与预览一致(文字不再画到画外、位置不再上下颠倒);logo 复制进 Application Support;裁切水印开关只影响当次导出
- **v0.4.0** (2026-08-29) — 新增标记编辑器(MARKUP_PLAN.md 四期一次落地):文字标记(点放/拖动/双击改字/⌫删除)、画笔(6 色 3 档粗细,RDP 平滑)、马赛克与模糊(笔迹作蒙版,效果底图块级归一化,预览与导出同一绘制函数所见即所得)、橡皮擦;⌘Z 撤销重做;导出烙印保 EXIF/GPS。裁切导出新增水印选项(文字/透明 PNG logo、九宫格定位、透明度/大小、可平铺),配置在偏好设置,零依赖纯 CoreText/CoreGraphics 实现
- **v0.3.2** (2026-08-29) — 裁切升级:新增撤销/重做(⌘Z)、变换条(旋转 90°/水平垂直翻转/±45° 拉直,预览与导出同管线)、打开裁切带入主视图旋转角度、方向键微调选区(⇧ 加速)、比例新增 3:2/5:4/自定义宽高比并支持横竖交换、导出可限最长边像素、可选去掉 GPS 位置信息、可覆盖原图(仅同格式源,先写临时文件再原子替换)
- **v0.3.1** (2026-08-29) — 新增幻灯片播放模式:开启自动进入只看图,鼠标移到窗口底部浮现控制条(播放/暂停、前后切换、间隔循环、退出),静止后淡出;纯净模式下不开播也会唤出,可直接在其中开播;顶栏新增幻灯片播放/暂停按钮;间隔 1/2/3/5/10 秒(偏好设置 + HUD 循环切换),空格键全局播放/暂停;手动切图重置计时,模态面板打开自动暂停,不循环时播到端点停在当前张
- **v0.2.0** (2026-08-29) — 信息面板改挤压式扁平分区(布局冻结,画布不挤压);深色画布下侧栏深色磨砂 + 顶栏/侧栏亮色适配;缩放修复:捏合以视口中心为锚、锚点夹取坐标系混用导致的深缩放画面偏移、全尺寸换图延迟到动画结束(消除跳变与卡顿)、缩放读数语义统一;修复旋转角度跨纯净模式丢失、外部打开主线程扫盘、打印解码限纸张分辨率、删除确认与隐藏恢复等(UI_REVIEW.md 全部 P0/P1 清零)
- **v0.1.0** (2026-08-24) — 首个可发布预览(当时误标为 v0.10):磨砂侧栏/纯净顶栏/可拖中线/原生红绿灯/柔和配色/稳定性优化
- 更早见 `git log`

## 许可证

MIT · 零第三方依赖（SVG/JXL 等矢量/高画质为可选二期）

> GPL-3.0 项目 `FlowVision/iMonet/nomacs/qView/Art-Book` 禁止参考；可参考 MIT `imageviewer5/Binder/oculante`
